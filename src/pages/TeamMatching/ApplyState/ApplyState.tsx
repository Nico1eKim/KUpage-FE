import ApplyStateHeader from '../../../components/TeamMatching/ApplyState/ApplyStateHeader';

const ApplyState = () => {
  return (
    <div className="w-full h-full flex flex-col bg-gray">
      <ApplyStateHeader />
      <div className="flex-1 mt-8 bg-darkblue"></div>
    </div>
  );
};

export default ApplyState;
